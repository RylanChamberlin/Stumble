import { StyleSheet } from "react-native";

export default StyleSheet.create({
  
    input: {
        padding: 10,
        backgroundColor: "white",
        borderRadius: 20,
        margin: 10
    },

})